# Detector unit/integration tests intentionally contain fake secret-shaped
# fixtures (same as upstream TruffleHog). Exclude them from push protection.
paths-ignore:
  - "**/*_test.go"
  - "**/*_integration_test.go"
